<form method="post" action="{{ path('course_delete', {'id': course.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course.id) }}">
    <button class="btn">Удалить</button>
</form>
