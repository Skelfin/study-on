{% extends 'base.html.twig' %}

{% block title %}Новый урок / StudyOn{% endblock %}

{% block body %}
<div class="container mt-2">
    <h1 class="mb-3">Новый урок</h1>
    <p>Курс: <a href="{{ path('course_show', {'id': form.vars.value.course.id}) }}" class="link-hover">{{ form.vars.value.course.name }}</a></p>

    {% include 'lesson/_form.html.twig' %}

</div>
{% endblock %}
