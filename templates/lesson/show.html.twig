{% extends 'base.html.twig' %}

{% block title %}{{ lesson.name }} / {{ lesson.course.name }} / StudyOn{% endblock %}

{% block body %}
<h1>{{ lesson.name }}</h1>
<p>Курс: <a href="{{ path('course_show', {'id': lesson.course.id}) }}">{{ lesson.course.name }}</a></p>
<p>{% autoescape 'html' %}{{ lesson.content|raw }}{% endautoescape %}</p>

<a href="{{ path('lesson_edit', {'id': lesson.id}) }}">Редактировать урок</a>
{{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
